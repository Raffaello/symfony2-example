Hello!

{% if is_granted('IS_AUTHENTICATED_FULLY') %}
    <a href="{{path('sonata_user_security_logout')}}">Logout</a>
    <a href="{{path('sonata_user_profile_show')}}">Profile</a>
{% elseif is_granted('ROLE_ADMIN') %}
    <a href="{{path('sonata_admin_dashboard')}}">Admin</a>
{% else %}
    <a href="{{path('sonata_user_security_login')}}">Login</a>
{% endif %}
